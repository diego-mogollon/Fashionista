<div class="navbar navbar-expand-sm navbar-light navbar-lewagon pt-3 px-5">
    <%= link_to render(:partial => 'shared/logo'), new_user_session_path %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <% if current_user.nil? %>
            <li class="nav-item">
                <%= link_to image_tag('woman.svg', class: 'auth-icon'), new_user_session_path %>
            </li>
            <% else %>
            <li class="nav-item active">
                <%= link_to "My collections", items_path, class: "nav-link" %>
                <%= link_to "logout", destroy_user_session_path, method: :delete %>
            </li>
        </ul>
        <% end %>
    </div>
</div>