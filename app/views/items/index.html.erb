<main>
  <div class="container">
    <div class="row">
    <%# <%= link_to 'Add New Clothes', new_item_path, class: 'btn btn-primary' %>
      <div>
        <% @items.each do |item| %>
         <p>Product Name: <%= item.name %> | Size: <%= item.size %> | Price: <%= item.price %> AUD</p>  
          <p>Description: <%= item.description %></p>
        <% end %>
      </div>
    </div>
  </div>
</main>
