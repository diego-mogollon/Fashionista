<%= simple_form_for item do |c| %>
  <%= c.input :name %>
  <%= c.input :size %>
  <%= c.input :price %>
  <%= c.input :description %>
  <%= c.submit "#{item.id ? "Edit" : "Add" } Item", class: "btn btn-success" %>
<% end %>
