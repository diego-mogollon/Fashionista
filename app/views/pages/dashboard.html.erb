<div class="container">
  <div class="row">
    <div>
      <% if @user.items.present? %>
        <% @user.items.each do |item| %>
          <%= item.name %>
        <% end %>
      <% else %>
        <p>You do not have any items</p>
      <% end %>
    </div>
  </div>
</div>
